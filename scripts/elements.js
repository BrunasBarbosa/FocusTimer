export {
  playButton,
  pauseButton,
  stopButton,
  setButton,
  plusButton,
  lessButton,
  buttons,
  treeButton,
  rainButton,
  shopButton,
  fireButton,
  minutesDisplay,
  secondsDisplay,
  backgroundCard,
  iconColor,
  sunButton,
  moonButton,
  html,
  body,
  inputColor,
  cards
}

const html = document.querySelector('html');

const body = document.querySelector('body');

const sunButton = document.querySelector('.light');

const moonButton = document.querySelector('.dark');

const playButton = document.querySelector('.play');

const pauseButton = document.querySelector('.pause');

const stopButton = document.querySelector('.stop');

const setButton = document.querySelector('.set');

const plusButton = document.querySelector('.plus');

const lessButton = document.querySelector('.less');

const buttons = document.querySelectorAll('.button');

const cards = document.querySelectorAll('.card');

const treeButton = document.querySelector('.tree');

const rainButton = document.querySelector('.rain');

const shopButton = document.querySelector('.shop');

const fireButton = document.querySelector('.fire');

const minutesDisplay = document.querySelector('.minutes');

const secondsDisplay = document.querySelector('.seconds');

const backgroundCard = document.querySelectorAll('.card-background');

const iconColor = document.querySelectorAll('.icon-color');

const inputColor = document.querySelectorAll('.volume');